# Actix-web bind address configuration
APP_HOST=0.0.0.0
APP_PORT=8080

# Connection string for MySQL using the Diesel CLI format
DATABASE_URL=mysql://movievault:password@127.0.0.1:3306/MovieVaultDB

# Log level for env_logger (info, debug, trace, etc.)
RUST_LOG=info

