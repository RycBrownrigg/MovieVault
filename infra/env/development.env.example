# ---------- Database (MySQL) ----------
MYSQL_ROOT_PASSWORD=movievault_root
MYSQL_DATABASE=MovieVaultDB
MYSQL_USER=movievault
MYSQL_PASSWORD=movievault

# ---------- Backend (Actix + Diesel) ----------
APP_HOST=0.0.0.0
APP_PORT=8080
DATABASE_URL=mysql://movievault:movievault@db:3306/MovieVaultDB
RUST_LOG=info

# ---------- Frontend (Vite) ----------
VITE_API_BASE_URL=http://localhost:8080/api

